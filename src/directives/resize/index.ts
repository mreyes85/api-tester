import { DirectiveBinding } from "vue"
import { IResize } from "./types";

var Resize: IResize = {
    bind: function(el: HTMLElement, binding: DirectiveBinding, vnode: VNode){
        
    }
}

export default Resize.bind;